<?xml version="1.0" encoding="utf-8"?>

<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:padding="@dimen/map_marker_padding"
    android:layout_width="@dimen/map_marker_image"
    android:layout_height="@dimen/map_marker_image">

    <ImageView
        android:id="@+id/image"
        android:layout_width="@dimen/map_marker_image"
        android:layout_height="@dimen/map_marker_image"
        android:layout_gravity="center"
        android:gravity="center"/>

    <TextView
        android:id="@id/amu_text"
        android:layout_width="@dimen/map_marker_image"
        android:layout_height="@dimen/map_marker_image"
        android:layout_gravity="center"
        android:gravity="center"
        android:textStyle="bold"
        android:textSize="18sp"
        android:text="0"
        android:background="@drawable/background_radial_whiteout"/>

</FrameLayout>